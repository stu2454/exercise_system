# Development Progress — 2026-08-20

## 1. Session summary

**Capability:** Client Demo / GitHub Pages deployment
**Status:** ✅ Complete
**Tests:** 35 test files / 228 tests passing
**Developer production build:** Pass
**Client Demo production build:** Pass
**git diff --check:** Pass

### Outcome

Added a separate participant-only static application surface suitable for
GitHub Pages. It derives a one-circuit programme from the existing nine-exercise
configuration and shares the established runner, participant UI, camera, pose,
video, prompt and session-result services. The full local developer application
remains available as the normal development entry.

---

## 2. Build status

Build 7 remains complete through Stage 6. Stage 7 Session Detail View remains
⬜ Not started. The Client Demo is an additional completed deployment capability,
not a replacement or renumbering of Build 7.

---

## 3. Work completed this session

### Objective

Provide an external user with a simple static URL that opens directly on an
exercise landing screen and runs all nine exercises once without exposing the
developer workspace.

### Implemented

- Added a dedicated `client-demo/` Vite root and `ClientDemoApp` composition.
- Added a pure one-circuit programme derivation that changes only set count;
  exercise order, dose, duration, rest and demonstration settings are preserved.
- Reused the shared runner and participant experience; no second state machine
  or duplicated exercise definitions were introduced.
- Removed set/circuit labels, developer controls, raw session detail and
  implementation terminology from the Client Demo surface.
- Added a direct landing page with one **START PROGRAMME** action and a concise
  browser-processing privacy note.
- Kept camera, pose overlay, reference demonstrations, pause/resume and graceful
  exit. Expected camera, pose and video failures use participant-facing text.
- Made reference-video and natural-audio playback respect Vite's deployment
  base.
- Added separate local development, build and preview scripts.
- Added a GitHub Actions Pages workflow that tests, builds, uploads and deploys
  only `dist-demo/`.

### Data/model changes

No shared schema changes. Normal `ProgrammeSessionResult` generation remains in
use internally. No backend, account, upload or remote persistence was added.

### Material files

- `client-demo/index.html`, `client-demo/src/client-demo-main.tsx`
- `src/ui/ClientDemoApp.tsx`, `src/ui/ClientDemoLanding.tsx`
- `src/clientDemo/clientDemoProgramme.ts`
- shared participant/video/audio base-path integration
- `vite.config.ts`, `package.json`, `.github/workflows/deploy-client-demo.yml`
- README, Client Demo documentation and project status

---

## 4. Decisions made

| Decision | Rationale |
|---|---|
| Separate Vite root | Excludes the developer React application from the demo dependency graph instead of hiding it with CSS. |
| Derive, do not duplicate, the programme | Preserves one authoritative exercise/programme model and shared engine. |
| Override sets to one only | Produces the requested linear demo while retaining duration, rest and multi-round support elsewhere. |
| No routes | A single root page avoids GitHub Pages SPA fallback problems. |
| Resolve playback assets through `BASE_URL` | Supports localhost, domain-root and repository-subpath deployments. |
| Retain CDN pose assets | Keeps this stage small and static; offline/self-hosted MediaPipe is a later option. |

---

## 5. Bugs identified and fixed

### Root-relative media under GitHub Pages

**Cause:** Canonical video and prompt paths begin at `/videos/` and `/audio/`,
which points at the domain root on a project Pages site.
**Fix:** Resolve the path against Vite's build base at the browser playback
boundary while retaining canonical library paths and validation.

---

## 6. Architecture impact

```text
Developer index → Developer App ┐
                                ├→ Shared exercise/runner/camera/pose/session services
Client Demo index → Demo App ───┘
```

No sensing, filtering, movement or runner algorithm changed.

---

## 7. Persistence impact

No new persistence. The Client Demo creates the normal structured result in
memory but exposes no session inspection, diagnostic recording or export UI.
Camera video and pose data are not uploaded or stored remotely.

---

## 8. Verification

- `npm test`: 35 files, 228 tests passed.
- `npm run build`: passed; developer output generated in `dist/`.
- `npm run build:demo -- --base /exercise_system/`: passed; static output
  generated in `dist-demo/`.
- Generated demo output contains `index.html`, JS/CSS and all nine video files.
- Generated `index.html` uses `/exercise_system/` asset URLs.
- Generated demo JavaScript contains none of the searched developer navigation,
  editor, replay, test-run, `+1 Rep`, or launch-mode labels.
- `git diff --check`: passed.

---

## 9. Manual verification

- Static output structure and repository-base URLs inspected.
- A real camera/pose/autoplay browser smoke test was not possible in the
  command-line environment and remains required on localhost and deployed HTTPS.
- A timed nine-minute exercise run was not manually completed; the exact
  sequence and terminal transition were verified through shared-runner tests.

---

## 10. Known limitations

- MediaPipe WASM/model files still load from public CDNs, so first use needs
  network access even though the deployed application itself is static.
- Native `.mov` playback depends on browser codec support; participant-friendly
  fallback text is shown if playback fails.
- Browser permission and autoplay differences require smoke testing in Chrome,
  Safari and Edge on the deployed HTTPS URL.
- GitHub Pages must be manually configured to use GitHub Actions as its source.

---

## 11. Next recommended step

Deploy the workflow, perform the cross-browser HTTPS smoke test, and then resume
the already planned Build 7 Stage 7 Session Detail View. Do not infer deployment
success until the repository workflow and Pages environment have run.

---

## 12. Handover

The Client Demo is built with `npm run build:demo`; GitHub Actions supplies the
repository base path automatically. The normal `npm run dev` remains the full
developer application. Before changing runner behaviour, verify both the
one-circuit demo tests and the existing 27-execution development-programme test.
